<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.go.kaptnet.board.mapper.BoardMapper">
    <select id="findAll" resultType="kr.go.kaptnet.board.dto.BoardDto">
        SELECT id, title, content, author, created_at, updated_at
        FROM board
        ORDER BY created_at DESC
    </select>

    <select id="findById" resultType="kr.go.kaptnet.board.dto.BoardDto">
        SELECT id, title, content, author, created_at, updated_at
        FROM board
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="kr.go.kaptnet.board.dto.BoardDto"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO board (title, content, author)
        VALUES (#{title}, #{content}, #{author})
    </insert>

    <update id="update" parameterType="kr.go.kaptnet.board.dto.BoardDto">
        UPDATE board
        SET title = #{title}, content = #{content}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM board WHERE id = #{id}
    </delete>
</mapper>
